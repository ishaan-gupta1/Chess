# Chess
Chess app made in C++, decided to thug it all in one .cpp file, uses FEN to create boards. FEN string then turns into GameState struct.

Currently has essentially all normal chess features, check, checkmate, en passant, castling, and can move back and forth between moves.

Click pieces to move.
Press 'R' to restart.
Use arrow keys to move back in forth in moves.
